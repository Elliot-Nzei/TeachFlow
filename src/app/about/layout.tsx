
export const metadata = {
  title: 'About TeachFlow',
  description: 'Learn more about the mission and vision behind TeachFlow.',
};

export default function AboutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
